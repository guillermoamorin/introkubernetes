# Default values for myOdoo.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

postgres:
  config_map_name: postgres-config
  postgres_db: postgresdb
  postgres_user: odoo
  postgress_pass: odoo
