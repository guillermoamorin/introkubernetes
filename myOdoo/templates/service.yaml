apiVersion: v1
kind: Service
metadata:
  name: {{ include "myOdoo.fullname" . }}
  labels:
    {{- include "myOdoo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myOdoo.selectorLabels" . | nindent 4 }}
